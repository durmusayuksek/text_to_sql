# Project Rules

- Use modular architecture
- Never place SQL generation inside API routes
- All SQL must go through sql_validator.py
- Business logic belongs in /modules
- Frontend components should remain reusable
- Use TypeScript interfaces
- Use async FastAPI endpoints
- Keep functions small and testable
